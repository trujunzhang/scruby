<%= form_for(@haraj) do |f| %>
  <% if @haraj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@haraj.errors.count, "error") %> prohibited this haraj from being saved:</h2>

      <ul>
      <% @haraj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
